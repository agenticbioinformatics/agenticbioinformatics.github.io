Prompt History
==============

#1 — 2026-05-12
Prompt:
  Build a simple GitHub Pages website for storing projects with repository links.
  Dark gray background with a double helix DNA animation in gradient from blue to green.
  Short description on top. Two sections: left with projects (name, link, description),
  right with projects (name, link only). Right section dimmed to draw attention to the left.

Changes:
  - Created index.html
  - Animated DNA double helix on a full-screen canvas (blue→green gradient strands,
    rungs, and nodes, rotating continuously)
  - Dark #1a1a1e background, two-column grid layout
  - Hero section with gradient title and placeholder description
  - Left column: 4 placeholder project cards with name, GitHub link, and description;
    full opacity with hover lift + green border accent
  - Right column: 5 placeholder project cards with name and link only;
    set to ~38% opacity, brightens to 80% on hover
  - Responsive: columns stack on narrow screens

--------------------------------------------------------------------------------

#2 — 2026-05-12
Prompt:
  Create a file prompt_history with my prompts (including this one) and summary
  of changes for each prompt.

Changes:
  - Created prompt_history (this file)

--------------------------------------------------------------------------------

#3 — 2026-05-13
Prompt:
  Format the description on the website, but don't change the text. Repositories
  on the left should correspond to repositories on the right — add a connecting
  line. Add a possibility that in the future one project on the left might
  correspond to several projects on the right. Add a footnote that says
  "Designed by Jędrzej Kubica [hyperlink to https://github.com/jedrzejkubica]
  · Crafted with Claude Code [hyperlink to https://claude.com/product/claude-code]."

Changes:
  - Description restructured into two <p> paragraphs + a styled "Couple of rules:"
    heading + <ol> list with CSS counters (1), 2), …); text unchanged
  - Example quotes in rules 2 and 3 displayed as indented block-quote spans
  - SVG overlay (#connector-svg) added as absolute-positioned element inside
    .mapping-wrapper; JS draws cubic bezier curves from the right-center of each
    left card to the left-center of its connected right card(s)
  - connectionMap array in JS defines all left→right relationships; supports
    one-to-many (e.g. repo-name-4 → repo-name-8 + repo-name-9)
  - Hover interaction: mousing over a left card brightens its line(s) to full
    opacity and adds .connected-hover (blue border, full opacity) to the
    corresponding right card(s)
  - Right column opacity moved from .col-right to individual .col-right .project-card
    so hover highlighting can override per-card
  - Footer added with "Designed by Jędrzej Kubica · Crafted with Claude Code"
    and hyperlinks
  - Connector lines hidden on mobile (<720 px) where columns stack

--------------------------------------------------------------------------------

#4 — 2026-05-13
Prompt:
  Create a file called prompt_history with my prompts (including this one) and
  a summary of changes associated with each prompt (or add this information to
  an existing one).

Changes:
  - Added prompts #3 and #4 to this file

--------------------------------------------------------------------------------

#5 — 2026-05-13
Prompt:
  Move the DNA double helix to the right. Move from index.html project names,
  links and descriptions to two separate files, one for each column, for easy
  updates. Add a hyperlink to the website title "Agentic Bioinformatics" to
  https://github.com/agenticbioinformatics/virussusceptibility.

Changes:
  - DNA helix center shifted from W/2 to W*0.75 (right quarter of the viewport)
  - Project data extracted from index.html into two JSON files:
      data/agentic-projects.json  — left column (id, name, url, descriptions[])
      data/initial-projects.json  — right column (id, name, url)
  - index.html left/right column bodies replaced with empty container divs
    (#col-left-cards, #col-right-cards); cards are now rendered at runtime by JS
    via Promise.all(fetch(...)) loading both JSON files
  - Title "Agentic Bioinformatics" wrapped in <a> linking to
    https://github.com/agenticbioinformatics/virussusceptibility (gradient
    styling preserved, dims to 75% opacity on hover)
  - Added .project-desc p + p { margin-top: 6px } for paragraph spacing in
    description blocks (fixing previously invalid nested <p> markup)

--------------------------------------------------------------------------------

#6 — 2026-05-13
Prompt:
  Create or update a text file called "prompt_history" with my prompts
  (including this one) and a summary of changes associated with each prompt.

Changes:
  - Added prompts #5 and #6 to this file
